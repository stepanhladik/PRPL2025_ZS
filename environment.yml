name: prpl_golem
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - pandas
  - matplotlib
  - scipy
  - h5py
  - pillow
  - notebook
  - ipykernel
  - pip
  - pip:
    # Install the local package in editable mode after environment creation
    # Run: pip install -e ./golem_data_loader
    - "" 
